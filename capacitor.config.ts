import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aiappbuilder.ailearningcompanioncleanmobilebuild1748731526208rldr22j1a',
  appName: 'ai-learning-companion-clean',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https'
  }
};

export default config;